As we intend to predict something course over time, we'll want to steer away from classification, as it gives you more of a yes or no, hot, or cold type prediction. 
Regression, on the other hand, looks at the data given and calculates the general heading of the data points. 
This makes it "ideal" (at least in our use case) to predict the price of the stock in question.